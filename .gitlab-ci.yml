image: docker:latest

services:
  - docker:dind
# $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
before_script:
  - docker login -u $DOCKER_USER_TOKEN -p $DOCKER_USER_PASSWORD arayam/dit-825-age-detection
  - echo $CI_REGISTRY_USER
  - echo $CI_REGISTRY
stages:
  - build
  - test
  - deploy
# This section describes what shall be done to build and test the project.
build-and-test:
  tags:
    - docker-build
  stage: build
  script:
    - docker build -t app:latest .
    - docker images
   # - docker tag app:latest git.chalmers.se/courses/dit825/2022/group03
    - docker push arayam/dit-825-age-detection:app
    #- docker push app:latest
    # - docker run -t app:latest /bin/bash
    
    - exit

deploy:
  stage: deploy
  script:
    - kubectl apply -f kubernetes/deployment.yml

#deploy:
 # stage: deploy
  #script:
   # - docker pull registry.gitlab.com/<namespace>/<project-name>:latest
    #- docker run -it -d -p 8000:8000 registry.gitlab.com/<namespace>/<project-name>:latest
