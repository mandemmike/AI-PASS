image: python:3.8

variables:
  DJANGO_SETTINGS_MODULE: myproject.settings
  PYTHONPATH: $CI_PROJECT_DIR
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.pip_cache"

cache:
  paths:
    - .pip_cache/

stages:
  - test
  - build
#  - deploy

install_dependencies_&_test:
  stage: test
  script:
    - cd ./webservice
    - pip3 install pkg-config
    - pip3 install -r requirements.txt
    - python manage.py test

build_image:
  stage: build
  script:
    - docker build -t app .

#deploy:
 # stage: deploy
 # script:
  #  - docker push myproject
